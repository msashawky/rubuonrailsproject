<%= form_for(@site_admin) do |f| %>
  <% if @site_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_admin.errors.count, "error") %> prohibited this site_admin from being saved:</h2>

      <ul>
      <% @site_admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
